// Nuxt 最小配置文件。
// Nuxt 是「约定优于配置」的框架，绝大多数功能零配置即可用，
// 这里只打开开发者工具（浏览器里会出现 Nuxt DevTools 悬浮按钮）。
// https://nuxt.com/docs/api/nuxt-config
export default defineNuxtConfig({
  // 开发者工具：查看路由、组件、payload、性能等。仅开发环境生效。
  devtools: { enabled: true },
})
