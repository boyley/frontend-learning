/**
 * Next.js 配置文件。
 *
 * 这里保持"空配置"即可（对象里没有任何字段），
 * 只是为了演示：项目根目录下的 next.config.js 会被 Next 自动读取。
 * 常见可加的字段：图片域名白名单、重定向、环境变量、实验特性等。
 *
 * 注意：next.config.js 运行在 Node 环境，用的是 CommonJS（module.exports），
 * 而不是 ESM 的 export default。
 *
 * @type {import('next').NextConfig}
 */
const nextConfig = {};

module.exports = nextConfig;
