{
  "name": "07-vitest",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Vitest：Vite 原生、ESM 优先、与 Jest API 高度兼容的现代测试框架",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:ui": "vitest --ui",
    "coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "vitest": "^2.1.0"
  }
}
