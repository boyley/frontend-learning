// 引入 @testing-library/jest-dom：给 expect 扩展一批语义化的 DOM 断言
// 例如 toBeInTheDocument() / toBeDisabled() / toHaveTextContent() / toHaveClass()
// 官方：https://github.com/testing-library/jest-dom
require('@testing-library/jest-dom');
