{
  "name": "11-sass",
  "version": "1.0.0",
  "private": true,
  "description": "前端学习合集 · Sass/SCSS 预处理器教学工程",
  "scripts": {
    "build": "sass --no-source-map --style=expanded ./:./",
    "watch": "sass --no-source-map --style=expanded --watch ./:./"
  },
  "devDependencies": {
    "sass": "^1.77.0"
  }
}
