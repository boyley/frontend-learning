/**
 * noop-defer.js —— 一个被 <script defer> 引入的轻量脚本
 * ----------------------------------------------------
 * 对比 bad.html 里放在 <head> 的"同步阻塞脚本"，
 * 这里用 defer 引入：脚本会在 HTML 解析完成之后、DOMContentLoaded 之前
 * 按文档顺序执行，因此完全不阻塞首屏渲染，FCP / LCP 提前。
 * 这里只做一点无关紧要的初始化，证明"脚本照样能跑，但不拖慢渲染"。
 */
console.log('[good] defer 脚本已在解析完成后执行，未阻塞首屏渲染');
